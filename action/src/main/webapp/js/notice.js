/**
 * 有一种思路是在jsp 页面中设置一个隐藏的input ,id 固定.这个input 的value 读取后台的值;然后在js 文件中读取这个id 的值来输出
 */

/*if('${Message}'!=null && '${Message}'!=""){
	alert('${Message}');
//	location.href="${pageContext.request.contextPath}/getRateList.do";
}
*/